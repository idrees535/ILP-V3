from tqdm import tqdm
import time  # Just for simulating work in the loop

# Example loop with progress bar
for i in tqdm(range(100), desc="Processing items"):
    # Simulate some work with sleep
    print("ddddddd")
    #time.sleep(0.1)
    # Your loop code here
